{
  "name": "sjmc-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:frontend": "vite",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm install && cd backend && npm install && npm run build && cd .. && tsc && vite build",
    "start": "node backend/dist/backend/server.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@types/node": "^24.5.2",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "date-fns": "^3.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.9.2",
    "vite": "^5.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
